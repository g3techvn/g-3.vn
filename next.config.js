/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'congthaihoc.vn', 'product.hstatic.net', 'img.youtube.com'],
  },
}

module.exports = nextConfig 